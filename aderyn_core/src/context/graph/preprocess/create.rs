use crate::{
    ast::ContractDefinition,
    context::{graph::*, workspace::WorkspaceContext},
};

pub fn create_raw_callgraphs(context: &WorkspaceContext) {}

pub fn _create_raw_callgraph(
    context: &WorkspaceContext,
    base_contract: &ContractDefinition,
) -> Option<RawCallGraph> {
    None
}

